# NEXT_STEPS — 作業計画と未決定の論点

ADR-0001〜0004 で **コンセプトと中核設計** は確定済み。
ADR-0005〜0007 で **データの持ち方・駅ページの骨格・観察メモ**、ADR-0008 で **保存先（Notion）**、ADR-0009 で **オーサリング手順（ローカル先行＋一括投入）** を確定した。
ここから先に必要な作業と、まだ決めていない論点を整理する。

> **進め方の変更（2026-06-19, ADR-0009）**: 駅データは Notion 直書きをやめ、`data/stations/<駅>.json` に先に書いて `scripts/import-stations.ts` で一括投入する。語彙は `data/vocab.ts` が単一ソース。サブエージェントは Notion を触らずローカル JSON を書く。**未投入の新規駅はローカルに溜まる**（Notion 反映は import 実行時）。

> **進め方の変更（2026-06-18）**: 当初は Phase 0（語彙確定）を最初の一手にしていたが、
> 「タグの粒度は箱（DBの形）から決めたい」という判断により、**データモデル設計（Phase 1）を語彙確定より先に**回す。
> 語彙（アンカー／ショックのマスタ）は、箱が決まってから粒度を確定する。

---

## 0. まだ決まっていない論点（先に意識しておく）

- ~~**保存先／スタック未定**~~ → **Notion 先行で確定（ADR-0008）**。既存DB「東京100駅経済観察マップ」を再利用。アプリ実装は型が固まってから別ADRで。
- ~~**類型の作り方**（スキーマ先行 vs ボトムアップ）~~ → **決着（ADR-0011）**: ボトムアップで立ち上げ済み（111駅で一巡）。以後は**ソフトフリーズ**＝新タグは「既存で書けない」提示＋ADR記載をゲートに足す。
- **答え合わせ用の史実データソース**: 過去の「答え」（＝3000字本文、ADR-0005）を誰がどこから書くか（自分で書く／Web／文献）。コストと信頼性のバランス。
- **成長度の測り方**: 観察プロキシ（目視）だけでいくか、データ（人口・地価・開業率など）も併用するか。

### 今回の対話で確定した論点（ADR化済み）

- ✅ **街は生成せず溜める／タグはフラット**（ADR-0005）: L3「生成ルール」は「分類タグの語彙」と読み替え。richness は3000字本文が持つ。
- ✅ **駅ページの骨格＝ループ順**（ADR-0006）: ①観察お題 ②賭け ③答え合わせ（供給/需要分離）④ズレ ⑤未来=観点。
- ✅ **観察メモ＝訪問ごとの時系列ログ**（ADR-0007）: 生テキスト保持。AI は「いったん」入れない（YAGNI）。将来やるなら相棒型・後付け。
- ✅ **保存先＝Notion 先行・ただし変数として持つ**（ADR-0008）: アプリを作る前提なので、Notion は「アプリが API で取り出す変数の置き場（CMS）」。本文も各パートを個別プロパティ（`観察お題`/`供給の筋`/`需要の筋`/`ズレ`/`未来の観点`）に分解。タグは multi-select（アンカー/ショック/需要/類型/ズレ類型/路線）。ループUI・賭け入力・メモはアプリ側で実装。
- ✅ **秋葉原を1駅エンドツーエンドで変数化**（パイロット完了）: 全パートを Notion プロパティに格納済み。
- ✅ **神保町を2駅目として変数化**（語彙検証）: ズレ類型が秋葉原と逆（需要が去って供給だけ残存）。検証の結果、語彙に不足が見つかり追加した → 需要 `本・知識`、類型 `古書店街型`、アンカー `出版・印刷`、路線 `東京メトロ半蔵門線`。**語彙はボトムアップで増やす運用が回り始めた**。
- ✅ **浅草・東京（丸の内）を3・4駅目として変数化**（サブエージェント並列で執筆）: さらに語彙が増えた → 類型 `興行・歓楽街型`（浅草六区）/ `オフィス街型`（丸の内）、ズレ類型 `外来需要の再来型`（浅草＝国内需要が去った後にインバウンドが外から再来）、路線 `東京メトロ丸ノ内線`。**ズレ類型が4種→5種に拡張、語彙検証が機能している**。
  - 保留した提案: アンカー `練兵場跡`（既存 `軍・基地跡` でカバーと判断し追加せず）、路線 `東京メトロ銀座線`/`東武`（浅草本文に登場。今後の駅で必要になれば追加）。
- ✅ **アプリ用の型を定義**: `docs/station.ts`（`Station` 型＋タグの string-literal union ＋ `mapNotionPageToStation()` マッパー雛形）。Notion プロパティ→アプリ変数のマッピングを固定。union は最新語彙に追従済み。
- ✅ **さらに6駅をサブエージェント並列で変数化**: 御茶ノ水・蒲田・日暮里・大手町・錦糸町・成城学園前。語彙がさらに拡張：
  - アンカー `病院・医療機関`／需要 `楽器・音楽`・`医療`・`繊維・手芸`・`ものづくり発注`／類型 `町工場集積型`・`専門卸街型`／路線 `東京メトロ千代田線`・`東京メトロ東西線`・`小田急線`
- ✅ **さらに6駅を並列で変数化**: 麻布十番・西新井・御徒町・大井町・神谷町・浅草橋。語彙追加：アンカー `鉄道用地・車両基地`／需要 `宝飾・貴金属`／路線 `東京メトロ南北線`・`東武スカイツリーライン`・`東武大師線`・`東急大井町線`・`りんかい線`。
- ✅ **さらに6駅を並列で変数化**: 大泉学園・石神井公園・綾瀬・東陽町・北千住・多摩川（田園調布）。語彙追加：アンカー `埋立・干拓地`・`撮影所・スタジオ`／需要 `アニメ・映像`・`行楽・レジャー`／路線 `西武池袋線`・`JR常磐線`・`東急東横線`・`東急目黒線`・`東急多摩川線`。
- ✅ **さらに6駅を並列で変数化**: 上野・豊洲・下北沢・王子・月島・自由が丘。語彙追加：アンカー `闇市・マーケット起源`／需要 `文化・芸術`・`演劇・舞台`・`古着`／類型 `大工場・産業発祥型`・`ブランド商店街型`／路線 `銀座線`・`京成本線`・`有楽町線`・`ゆりかもめ`・`都電荒川線`・`京王井の頭線`。
- ✅ **さらに6駅を並列で変数化**: 渋谷・池袋・中野・巣鴨・清澄白河・蔵前。語彙追加：需要 `IT・スタートアップ`・`信仰・参詣`・`シニア・高齢者`／類型 `クリエイティブ転換型`（倉庫/問屋→カフェ・クラフトの再生＝再頻出）／路線 `副都心線`・`田園都市線`・`JR埼京線`・`東武東上線`・`JR中央線`。
- 📊 **現在34駅が変数化済み**（秋葉原・神保町・浅草・東京・御茶ノ水・蒲田・日暮里・大手町・錦糸町・成城学園前・麻布十番・西新井・御徒町・大井町・神谷町・浅草橋・大泉学園・石神井公園・綾瀬・東陽町・北千住・多摩川・上野・豊洲・下北沢・王子・月島・自由が丘・渋谷・池袋・中野・巣鴨・清澄白河・蔵前）。`docs/station.ts` の union と `docs/patterns.md` も全駅・全語彙に追従済み。

### 📖 横断パターンは `docs/patterns.md` に集約

10駅時点で立ち上がった「街の変わり方の型」（ズレ類型4分類・アンカー→類型の傾向・専門集積クラスタ・知識産業の立地）を `docs/patterns.md` に書き出した。**駅を追加するたびに更新する**。これが ADR-0002 の狙った「システム理解」の出力。

### ✅ DB 全駅の本文化が完了（ADR-0009 のパイプラインで）

`pnpm run import` で全駅を投入し、**Notion DB の111ページすべてが本文6項目＋タグ＋区/カテゴリ＋おすすめルートを完備**（`pnpm run audit` で欠落0・重複0を確認）。内訳＝当初直接投入34駅 ＋ ローカル先行で量産77駅。
- データの真実の源は `data/stations/<駅>.json`（Git管理）。再投入は冪等。新駅は「JSON書く→`pnpm run import`」だけ。
- `おすすめルート`/`観察ポイント` は import の任意フィールド（値がある時だけ設定・空で上書きしない）。
- 監査ツール `pnpm run audit`（重複・欠落・スタブ検出）も常備。

量産を通じて語彙（`data/vocab.ts`）が大きく拡張した（アンカー25・ショック16・需要35・類型11※統合後・ズレ類型6・路線45＋）。新しい**ズレ類型 `稼働時間の偏り`**（官庁街・オフィス街の昼夜偏在）も主要軸として定着。

### ✅ 出発前プラン（ADR-0018）— ローカル77駅ぶん収録・Notion反映済み

くじ→即出発を成立させるため、`定番スポット`/`さくっとコース`/`しっかりコース`（任意 rich_text）を追加し、駅ループの行きフェーズから開示する（PR #6）。ローカル JSON の77駅は全駅収録・Notion 反映済み。

- **残作業**: Notion にのみ存在する当初直接投入の34駅はプラン未収録。対応するにはローカル JSON 化（真実の源の一元化・ADR-0009）とセットでやるのが筋。着手時は別ブランチで。

### ✅ 語彙の整理（類型の統合）→ 決着（ADR-0010）

量産で保留提案が溜まり類型が15種まで発散していた問題を、**ADR-0010 で決着**させた。

- **類型 15種 → 11種** に統合。「特定産業の集積型」5種（サブカル集積型/古書店街型/専門卸街型/町工場集積型/大工場・産業発祥型）を新類型 **`専門集積型`** に畳んだ。**どの産業かは型でなく 需要/アンカー タグで表す**（二重表現の排除）。`大学集積型` は機関アンカーの別格性から例外的に存続。
- 保留中だった「専門◯◯型」提案は追加せず `専門集積型`＋タグで吸収。「再開発の下位種」（国際都心化/計画的副都心開発/臨海部開発/都市計画道路の貫通）も追加せず `大規模再開発`／`区画整理・用途地域` に集約。
- **実施済み**: `data/vocab.ts` の `ARCHETYPES` を11種に更新 / ローカルJSON＋Notion全ページを `専門集積型` に一括移行（`scripts/migrate-archetypes.ts`、26ページ更新）/ Notion スキーマから旧5オプションを除去 / `docs/patterns.md` の分布を更新（専門集積型26駅で4位タイ）。

**まだ保留（軽微・必要になったら判断）**:
- ズレ類型: `名残`（アンカー消失後もブランド残存＝学芸大学）/`周辺ターミナルへの需要流出`（東中野）
- アンカー: 煉瓦街・近代計画街区/芝居町・興行起源/文士村・文化人集住/生活軌道・路面電車/予備校・専門学校/園芸・植木集落
- 需要: 古本・古書/古道具・アンティーク/展示会・見本市/ラーメン・地域グルメ/社交・コミュニティ など細粒度

→ いずれも現状は既存タグ＋本文で表現できており、発散リスクは低い。実データで「既存語彙で書けない」駅が出たときに ADR で追記判断する。

- **路線マスタの設計**: 駅ごとに私鉄が増え続け、`都営線`を個別線（大江戸/新宿/三田/浅草）に割るか、`京急本線/空港線`等の粒度を揃えるか要検討。multi_select 継続か別マスタ化か。
- ~~**patterns.md の更新**~~ → **111駅版に更新済み**（`pnpm run stats` で再集計可能）。新パターン（クリエイティブ転換型16・稼働時間の偏り14・埋立再開発・供給先行の二極化・外来需要の再来17）を反映。

---

## 1. フェーズ別タスク

### Phase 0 — 語彙（マスタ）の確定 ※Phase 1（箱）の後に回す
ADR-0002 の分類タグ語彙の本体（ADR-0005 で「生成ルール」→「分類タグ語彙」と読み替え）。アンカーとショックの型を確定させる。
**タグはフラット**（ADR-0005）なので、粒度は「種類名」だけで決まる。箱の形が見えてから粒度を確定する。

- [ ] **アンカー種別マスタ** を10個前後でFIX（命名・粒度を決める）
- [ ] **ショック種別マスタ** を10個前後でFIX
- [ ] 「地価／区画」を独立軸にするか、各型の属性に畳むか決める
- [ ] 確定した語彙で、既知の数駅（神保町・秋葉原など）にタグを貼って破綻しないか検証

#### 叩き台（アンカー）
1. 寺社（浅草・門前仲町）
2. 宿場・街道（品川・新宿・千住）
3. 城・武家地（番町・麹町）
4. 港・河岸・物流結節（日本橋魚河岸・月島）
5. 大学・学校（神保町・本郷・御茶ノ水）
6. 官庁（霞が関・永田町）
7. 鉄道ターミナルそのもの（渋谷・池袋）
8. 工場・産業立地（蒲田・王子）
9. 軍・基地跡（六本木・代々木）
10. 卸・問屋街（馬喰町・合羽橋）
- 検討中: 「計画型（財閥／地主開発）」＝丸の内・田園調布 を11個目に入れるか

#### 叩き台（ショック）
1. 関東大震災(1923)
2. 戦災・空襲＋闇市（秋葉原・新橋・上野）
3. 鉄道・地下鉄の開通／延伸
4. 区画整理・用途地域（都市計画）
5. バブルと崩壊（地上げ・地価）
6. 大規模再開発（六本木ヒルズ・大丸有・湾岸タワマン）
7. オリンピック(1964 / 2020)
8. 脱工業化・工場移転（跡地のタワマン化）
9. EC化・ネット（神保町古書・秋葉原家電）
10. インバウンド（浅草・銀座）

### Phase 1 — データモデル設計（＝箱）★まずここ
タグの粒度を決める前に、箱の形を確定させる（進め方の変更を参照）。
- [ ] マスタ: `anchor_type` / `shock_type` / `archetype`(類型) ※**フラットタグ**（ADR-0005）
- [ ] エンティティ: `station`(駅) を **供給をルート** に設計（ADR-0003）
- [ ] リレーション: 駅 × アンカー、駅 × ショック、駅 → 類型（いずれもタグ付けの join）
- [ ] 本文: 過去〜現在（③④用・隠す）と 未来の問い（⑤用・見せる）を分けて持つ（ADR-0006）。3000字以内（ADR-0005）
- [ ] 需要（欲求）を **オーバーレイ** としてどう持つか
- [ ] 「供給と需要のズレ」をどう表現・記録するか（本文④で語る前提、ADR-0006）
- [ ] `walk_memo`: station にぶら下がる時系列ログ。`visited_at` / `body`(生テキスト) / `photo?` / `ai_analysis`(空スロット)（ADR-0007）

### Phase 2 — 駅1ページのスキーマ（記録項目）
1ページに何を持たせるか。骨格はループ順（ADR-0006）、時制は ADR-0004 に沿って:
- [ ] 基本情報（駅名・区・路線）
- [ ] ① 観察のお題（現在・答えは出さない）
- [ ] ② 賭け＝**自分の仮説** フィールド（なぜ／数年後）
- [ ] ③ 答え合わせ本文（過去〜現在・隠す・**供給/需要を分ける**）＋ 当たり/外れの記録
- [ ] ④ ズレ（供給と需要の食い違い）
- [ ] ⑤ 未来＝観点（仮想ショック注入／需給ギャップに何がハマるか）
- [ ] タグ（アンカー／ショック／類型）
- [ ] 観察メモ（時系列・写真）→ `walk_memo`（ADR-0007）

### Phase 3 — 仮説 → 答え合わせループのUX
- [ ] 「先に賭けてから史実をめくる」順序を強制する導線
- [ ] 外れたときに「なぜ外れたか」を残す欄
- [ ] 時制ごとに体験を変える（過去=答え合わせ／現在=観察入力／未来=観点提示）

### Phase 4 — スタック決定 & 最小実装
- [ ] 上の「未決定の論点」のスタックを決める（別ADR）
- [ ] まず1駅をエンドツーエンドで通す（縦に薄く）

### Phase 5 — 運用テスト & 反復
- [ ] 実際に数駅を歩いて記録 → 語彙・スキーマの破綻を洗い出して反復

---

## 最初の一手

箱（Notion）と秋葉原パイロットは完成。次は **本文を数駅ぶん書き溜めて、語彙（タグ）が破綻しないか検証する**（Phase 0 ＋ Phase 5 を一緒に回す）。
- 候補: 神保町（大学×EC化）・浅草（寺社×インバウンド）・丸の内（計画型×大規模再開発）など、ズレ類型が異なる駅を選ぶと語彙の穴が見つけやすい。
- 書きながら、足りないアンカー/ショック/需要/ズレ類型の選択肢を追記し、粒度を調整する。
