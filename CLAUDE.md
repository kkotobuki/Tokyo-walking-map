# CLAUDE.md（汎用ベース）

このファイルは複数プロジェクトで使い回すためのベーステンプレ。
プロジェクト固有のルールは末尾の「このリポジトリ固有のルール」セクションに分離して書く。

---

## 1. 破壊的操作の境界線（auto mode でも止まるべきもの）

Claude Code 標準のガードに加え、以下はユーザー明示指示時のみ実行する。

### 1.1 git
- `git push --force` / `--force-with-lease`（特に main / develop / release ブランチ）
- `git reset --hard`
- `git branch -D`（マージ前のブランチ削除）
- `git checkout .` / `git restore .`（未コミット変更の破棄）
- `git rebase -i`、hook スキップ（`--no-verify`, `--no-gpg-sign`）

### 1.2 ファイルシステム
- `rm -rf`、ディレクトリ単位の削除
- 大量ファイルの一括削除
- 既存ファイルの完全上書き（読まずに `Write` する等）

### 1.3 Docker / コンテナ
**確認なしで実行してよい**:
- `docker builder prune -af`（ビルドキャッシュ削除）
- `docker image prune`（ダングリング image 削除、`-a` は付けない）
- `docker ps -a` 等の状態確認系コマンド

**実行前に必ずユーザー確認**:
- `docker stop` / `docker rm` / `docker rmi` / `docker volume rm`
- `docker system prune -a`
- DB コンテナの停止・リセット系コマンド全般

### 1.4 データベース
- 本番への migration 適用（`prisma migrate deploy`, `supabase db push`, `alembic upgrade head` 等）
- DB リセット系コマンド（`db reset`, `DROP DATABASE`, `TRUNCATE` 等）
- 既存テーブルへの破壊的変更を含む migration の作成

### 1.5 コスト発生する操作
- LLM API のバッチ・ループ呼び出し（数十回〜の繰り返し）
- クラウドリソースの作成（DB インスタンス、ストレージ、関数、コンテナ）
- 課金される外部 API の連続呼び出し

---

## 2. 本番環境への操作ルール

- 本番 DB への書込操作（`INSERT` / `UPDATE` / `DELETE` / `DROP` / `ALTER`）はユーザー明示指示時のみ
- 本番環境を指す `.env` / 設定ファイル / 接続 URL の書き換え禁止
- 特権キー（Service Role Key, Admin Key, root credentials, AWS Access Key 等）を使うコードを Claude 主導で書かない
- 本番環境への接続を要する検証は、必ず staging / local で代替できないか先に検討する

---

## 3. シークレット・依存管理

### シークレット
- `.env*` / `credentials.json` / `*.pem` / `*.key` 等は絶対にコミットしない
- API キー・トークンをコード内にハードコードしない
- ログ・エラーメッセージにシークレットを出力しない
- **環境変数の値を Claude との会話（チャット）に貼ることを促さない**
  - 会話履歴・キャッシュ・テレメトリ経由で漏洩するリスクがあるため
  - 共有が必要な場合は「変数名」「存在するか」「型」のみを扱う
  - 値そのものが必要な処理は、ローカルファイル / `.env` 経由で読み込ませる

### 依存パッケージ
- 新規パッケージの追加はユーザー確認（用途・代替案の提示込み）
- メジャーバージョンアップは破壊的変更の可能性を調査してから提案
- 既存依存の削除・ダウングレードもユーザー確認

---

## 4. ブランチ / PR ルール

- **main への PR は develop ブランチからのみ** 作成する
- **main / develop への変更は PR 経由で行う**
- PR サイズは適切に分割する（レビュアーが追える単位に）
- WIP / fixup コミットは整理してから PR を作成する

---

## 5. 削除・クリーンアップ提案の作法

- **「最終ログ時刻」「最終更新時刻」だけで unused 判定しない**（内部通信だけで動くサービス、cron でしか叩かれないジョブ、稀にしか動かないバッチがある）
- **「停止」と「削除」は別物として扱う**（後者はデータ消失の可能性）
- データを伴うリソース（DB volume, ストレージバケット, ログ等）の削除前にバックアップ要否を確認
- 「使われていない」と判断する前に、grep / コードリファレンス / デプロイ履歴を最低 1 つは確認する

---

## 6. コミュニケーション・言語前提

- ユーザーへの応答は日本語（指示がない限り）
- 不確実な情報を「事実」として伝えない（確信度を添える）
- **このテンプレは日本語運用を前提とする**:
  - Skill / Command / Hook の発火トリガー語は日本語固定（多言語化はしない）
  - CLAUDE.md / README / CHANGELOG / コミットメッセージは日本語ベース
  - 英語環境向けに展開する場合は、テンプレ自体をフォークして翻訳する想定

---

## このリポジトリ固有のルール

### プロジェクト概要

**Walking App（仮）** — 知らない街を歩きながら「この街はなぜこうなったのか」を自分で考え、仮説を立てて残し、史実で答え合わせする思考装置。
観光アプリでも歩数アプリでもなく「東京というシステムを理解するゲーム」を目指す。既存の朝ニュース向け「思考シミュレータ」の“街版”（判断対象をニュースから「駅」に向け替えたもの）。

### 主要コマンド

- スタック未確定のため未定（package.json なし）。Phase 4 でスタック決定後に記入する。

### 技術スタック

- **未確定**。候補は Notion先行 / Next.js + Supabase / ローカル（Markdown・SQLite）の3案（→ NEXT_STEPS「未決定の論点」、別ADRで決定予定）。
- 普段の既定スタックは Next.js + Supabase。ただし ADR-0003 はDB設計の思想であって採用技術の決定ではない。
- 言語は TypeScript が基本。薄い抽象・直API志向。オーケストレーション系ミドルウェアは極力避ける。

### Git / PR フロー
<!-- デフォルト値。プロジェクト固有のルールがあれば編集 -->
- ベースブランチ: develop
- ブランチ命名: feature/{説明}, fix/{説明}, refactor/{説明}
- PR ベース: origin/develop
- main への merge: develop からのみ（共通ルール 4. 参照）

### 外部接続サービス
<!-- 開発中に追加していく。サービス名のみ記載、値は .env を参照 -->

---

## セッション開始時に読むもの

1. `docs/adr/` の ADR 4本（確定した設計判断）
2. `docs/NEXT_STEPS.md`（次の作業と未決定の論点）

迷ったらこの2つが正典。下の要約は索引で、詳細は必ず各ファイルを参照すること。

## 確定済みの中核判断（要約・詳細は各ADR）

- **ADR-0001**: 位置づけは「街版・思考シミュレータ」。答えを読む装置ではなく、自分の仮説を残し史実で答え合わせする装置。
- **ADR-0002**: ドメインは3層。駅 → 類型 → 生成ルール（アンカー × ショック × 地価）。
- **ADR-0003**: 供給エンジンをルートに、需要はオーバーレイ。両者の「ズレ」を読む。理由は因果でなく観測可能性。
- **ADR-0004**: 過去=答え合わせ／現在=成長度の観察／未来=観点提供に留める（計算で当てにいかない）。

## 現状と次の一手

コンセプトと中核設計は確定済み。次は **NEXT_STEPS の Phase 0＝語彙（アンカー/ショックのマスタ）確定**。叩き台は NEXT_STEPS に埋め込み済み。

## 勝手に決めないこと（未決定。確認してから動く）

- 保存先／スタック（Notion先行 / Next.js+Supabase / ローカル）
- 類型の作り方（スキーマ先行 or ボトムアップ）※ユーザー未回答
- 答え合わせ用の史実データソース

## 進め方の約束

- 確定判断は ADR に従う。**新しく設計判断をしたら必ず `docs/adr/` に新しいADRを追記する**（番号を継番）。後追いで腐らせない。
- 仕様や方針を変えるときは、理由とセットで該当ADR/NEXT_STEPSを更新する。
- 1機能ずつ「縦に薄く」通す。最初は1駅をエンドツーエンドで。

## 開発者メモ（好み）

- 会話は日本語（カジュアルでOK）。
- 言語は TypeScript が基本。普段の既定スタックは Next.js + Supabase（ただし本PJのスタックは未確定。ADR-0003 はDB設計の思想であって採用技術の決定ではない）。
- 薄い抽象・直API志向。オーケストレーション系ミドルウェアは極力避ける。
- 図は Mermaid。
